# .github/workflows/deploy-user.yml
# CI/CD automatizado para User Service

name: 🚀 Deploy User Service

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'backend/user-service/**'
      - '.github/workflows/deploy-user.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'backend/user-service/**'

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  SERVICE_NAME: user-service
  REGION: europe-west1

jobs:
  # ========================================
  # JOB 1: Análisis de Seguridad
  # ========================================
  security-scan:
    name: 🔒 Análisis de Seguridad
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔍 Escaneo de secretos con TruffleHog
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./backend/user-service
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --debug --only-verified

      - name: 📊 Análisis de vulnerabilidades (npm audit)
        working-directory: backend/user-service
        run: |
          npm audit --audit-level=moderate || true

  # ========================================
  # JOB 2: Deploy a Cloud Run
  # ========================================
  deploy:
    name: 🚀 Deploy a Cloud Run
    runs-on: ubuntu-latest
    needs: security-scan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🔐 Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: ☁️ Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: 🏗️ Build con Cloud Build
        run: |
          gcloud builds submit \
            --config=backend/user-service/cloudbuild.yaml \
            --substitutions=COMMIT_SHA=${{ github.sha }},_SERVICE_NAME=${{ env.SERVICE_NAME }} \
            .

      - name: 📝 Obtener URL del servicio
        id: get-url
        run: |
          SERVICE_URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region=${{ env.REGION }} \
            --format='value(status.url)')
          echo "url=$SERVICE_URL" >> $GITHUB_OUTPUT
          echo "✅ Servicio desplegado en: $SERVICE_URL"

      - name: 🏥 Health Check
        run: |
          sleep 30
          curl -f ${{ steps.get-url.outputs.url }}/health || exit 1
          echo "✅ Health check exitoso"

      - name: 📊 Resumen del Deploy
        run: |
          echo "## 🚀 Deploy Exitoso - User Service" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Servicio:** ${{ env.SERVICE_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Región:** ${{ env.REGION }}" >> $GITHUB_STEP_SUMMARY
          echo "- **URL:** ${{ steps.get-url.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Autor:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY

  # ========================================
  # JOB 3: Rollback automático
  # ========================================
  rollback:
    name: ⏪ Rollback (si falla)
    runs-on: ubuntu-latest
    needs: deploy
    if: failure() && github.ref == 'refs/heads/main'
    
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: 🔐 Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - name: ☁️ Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: ⏪ Rollback a versión anterior
        run: |
          echo "❌ Deploy falló, ejecutando rollback..."
          PREVIOUS_REVISION=$(gcloud run revisions list \
            --service=${{ env.SERVICE_NAME }} \
            --region=${{ env.REGION }} \
            --limit=2 \
            --format='value(name)' | tail -n 1)
          
          if [ -n "$PREVIOUS_REVISION" ]; then
            gcloud run services update-traffic ${{ env.SERVICE_NAME }} \
              --region=${{ env.REGION }} \
              --to-revisions=$PREVIOUS_REVISION=100
            echo "✅ Rollback completado a: $PREVIOUS_REVISION"
          else
            echo "⚠️ No hay versión anterior para rollback"
          fi